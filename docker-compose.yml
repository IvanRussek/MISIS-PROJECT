services:
  lomonosov_streamlit:
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ecstatic_mclaren
    environment:
      PORT: 8501
    ports:
      - "8501:8501"
